# sliceHW
Python slicing is used to extract a specific part of a sequence such as a string, list, or tuple. It uses the syntax `sequence[start:stop:step]`. The start index is included, while the stop index is excluded. Slicing makes it easy to select, reverse, or skip elements.
